function intervir (num: number) {
    let handrise = 0
    if (handrise == 0) {
        basic.showIcon(IconNames.Happy)
    } else {
        basic.showIcon(IconNames.Confused)
    }
}
radio.setGroup(1)
let aluno = 1
